import {DataTable} from "./DataTable.js";
export class GastosGeneralesDataTable extends DataTable {
  constructor(columnDefinitions, data, createOptions) {
    super(columnDefinitions, data, createOptions);
  }
}
